# TEST — copiar a deploy/secrets/.env.test (chmod 600, NO Git)
ACCIO_ENV=test
ACCIO_ENGINE_PORT=8193
PUBLIC_SITE_URL=http://127.0.0.1:8193

# Entorno CI o pruebas de integración. Datos mock o tenant de prueba.
