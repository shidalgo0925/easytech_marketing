# PROD — copiar a deploy/secrets/.env.prod (chmod 600, NO Git)
ACCIO_ENV=prod
ACCIO_ENGINE_PORT=8092
PUBLIC_SITE_URL=https://emaccion.etsrv.site

# Ver .env.example para el resto de variables (Odoo, LinkedIn, Meta, ACCIO_API_KEY…)
